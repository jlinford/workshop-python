# Use source-based instrumentation:
make CC=tau_cc.sh
./tau.sh

# View profiles on command line:
pprof | less

# View profiles in GUI:
paraprof

# Package profiles:
paraprof --pack profiles.ppk

# View packaged profiles:
paraprof profiles.ppk

